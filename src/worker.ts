// EmDash provides the Astro fetch handler and scheduled maintenance hooks.
// Plugins are disabled for the first production migration, so no Worker Loader
// binding is required at this stage.
export { default } from '@emdash-cms/cloudflare/worker';
